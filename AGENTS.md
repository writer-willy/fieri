# AGENTS.md — 《되는 것》 철학 형식화 프로젝트

## 0. 이 문서의 지위

이 문서는 《되는 것》 프로젝트에서 작업하는 모든 AI Agent가 따라야 하는 최상위 작업 규칙이다.

이 프로젝트는 일반적인 소프트웨어 제품 개발 프로젝트가 아니다.

Kotlin을 이용하여 철학적 개념, 개념 사이의 의존관계, 변환 규칙, 구성 관계, 상호의존 관계를 명시적으로 표현하고, 전체 철학 체계가 내부적으로 일관된 형태로 구성될 수 있는지를 탐구하는 프로젝트다.

따라서 이 프로젝트에서 중요한 것은 단순히 다음과 같은 것이 아니다.

* 코드가 실행되는 것
* 컴파일 오류가 없는 것
* 테스트가 통과하는 것
* 코드가 짧고 우아한 것
* 일반적인 소프트웨어 설계 원칙에 잘 맞는 것

가장 중요한 목표는 다음과 같다.

> 《되는 것》을 구성하는 철학적 개념들이 서로를 필요로 하면서, 정의되지 않은 도약이나 은폐된 범주 변경 없이 하나의 전체적 체계로 구성될 수 있는지를 Kotlin이라는 형식적 작업언어를 통해 검사한다.

따라서 Agent는 소프트웨어의 완성보다 철학적 구조의 보존을 우선해야 한다.

---

# 1. 프로젝트의 근본 목적

## 1.1 진리 증명이 아니다

이 프로젝트는 《되는 것》이 현실 세계에 대해 참이라는 것을 증명하기 위한 프로젝트가 아니다.

Kotlin compiler가 어떤 구조를 받아들였다는 사실은 그 철학적 명제가 현실에서 참임을 의미하지 않는다.

테스트가 통과했다는 사실 역시 경험적·과학적·형이상학적 진리를 증명하지 않는다.

이 프로젝트가 검사하려는 것은 우선적으로 다음과 같다.

> 내적 일관성

즉,

* 개념이 어디에서 필요한가
* 어떤 개념이 다른 개념을 전제하는가
* 어떤 변환이 허용되는가
* 어떤 변환이 허용되지 않는가
* 같은 이름이 서로 다른 의미로 몰래 사용되고 있지 않은가
* 서로 다른 층위가 설명 없이 동일시되고 있지 않은가
* 순환적 의존이 정당한 상호구성인지 순환논증인지
* 어떤 개념이 실제로 체계에 필요한지
* 개념을 제거했을 때 무엇이 무너지는지

를 검사하는 것이 목적이다.

---

# 2. Agent의 기본 역할

Agent는 이 프로젝트에서 다음 역할을 수행한다.

1. 사용자가 확정한 철학적 개념을 Kotlin으로 표현한다.
2. 개념 사이의 의존성을 명시적으로 만든다.
3. 타입을 이용해 범주 오류를 드러낸다.
4. 테스트를 이용해 철학적 invariant를 검사한다.
5. 리팩터링이 다른 개념에 미치는 영향을 추적한다.
6. 정의되지 않은 연결을 발견한다.
7. 중복되거나 실질적 기능이 없는 개념을 발견한다.
8. 동일한 형식이 다른 질료에서 반복되는 경우 이를 식별할 수 있도록 돕는다.
9. 철학적으로 결정되지 않은 문제를 사용자에게 돌려준다.
10. 코드베이스 전체가 현재 철학의 구조를 가능한 한 정직하게 반영하도록 유지한다.

Agent는 철학의 공동 설계 보조자일 수 있지만, 철학의 최종 결정권자는 아니다.

최종적인 철학적 판단은 사용자에게 있다.

---

# 3. 가장 중요한 금지 규칙

## 3.1 철학적 공백을 임의로 메우지 않는다

컴파일되지 않는다는 이유만으로 새로운 철학적 전제를 만들어서는 안 된다.

예:

```kotlin
fun 표상.기표화(): 기표
```

가 필요해 보이지만,

"왜 모든 표상이 기표화될 수 있는가?"

가 아직 철학적으로 결정되지 않았다면 이 함수를 임의로 추가하지 않는다.

대신 TODO 또는 명시적인 미결정 상태로 남기고 사용자에게 보고한다.

예:

```kotlin
/**
 * TODO(철학적 결정 필요)
 *
 * 표상에서 기표로의 변환이 항상 가능한지 확정되지 않았다.
 * 현재 체계만으로는 이 변환의 필요조건을 결정할 수 없다.
 */
```

---

## 3.2 컴파일을 위해 철학을 변경하지 않는다

다음과 같은 방법으로 오류를 숨기는 것을 금지한다.

* 무분별한 `Any`
* 무분별한 nullable
* 의미 없는 공통 상위 타입
* unchecked cast
* 강제 형변환
* 철학적으로 근거 없는 default 값
* 의미 없는 wrapper
* 모든 개념을 하나의 범용 객체로 통합
* 관계의 종류를 삭제하고 단순 문자열이나 enum으로 평탄화
* 미결정 상태를 성공 상태로 취급

컴파일 오류가 철학적 공백을 나타낸다면 오류는 제거 대상이 아니라 연구 대상이다.

---

# 4. 핵심 원칙: 컴파일 실패는 연구 결과일 수 있다

일반 소프트웨어에서는 컴파일 실패가 해결해야 할 문제다.

이 프로젝트에서는 반드시 그렇지 않다.

예를 들어 다음이 발견될 수 있다.

* `난쟁이`를 구성하려면 `역할`이 필요하다.
* `역할`을 구성하려면 언어적 `자리`가 필요하다.
* `자리`를 구성하려면 차이 구조가 필요하다.
* 차이를 관찰하려면 자기타자화가 필요하다.
* 자기타자화를 설명하려면 전체/부분 구조가 필요하다.

이때 컴파일 실패는 다음 사실을 드러낼 수 있다.

> 현재 철학적 체계에서 필요한 연결이 아직 정의되지 않았다.

Agent는 이를 임의로 해결하지 않는다.

가능한 경우 다음과 같이 보고한다.

```text
[철학적 컴파일 실패]

필요한 변환:
표상 -> 기표

현재 존재하는 정의:
부분 -> 표상
기표 -> 언어적 자리

누락:
표상이 어떤 조건에서 기표화되는지 정의되지 않음.

영향:
역할 생성 불가
난쟁이 생성 불가
의식 모델 구성 불가
```

---

# 5. Kotlin의 역할

Kotlin은 이 프로젝트에서 단순 구현 언어가 아니다.

Kotlin은 철학적 구조를 외부화하기 위한 형식적 작업언어다.

특히 다음 기능을 적극적으로 활용한다.

* 타입
* generic
* sealed interface / sealed class
* value class
* composition
* delegation
* extension function
* exhaustive `when`
* 불변성
* 테스트
* 모듈/패키지 의존성

그러나 Kotlin 자체의 구조를 철학에 강제로 투사해서는 안 된다.

철학적으로 적절하지 않다면 객체지향적 상속 구조를 억지로 사용하지 않는다.

---

# 6. 언어 정책

철학적 domain vocabulary는 원칙적으로 한국어를 사용한다.

예:

```kotlin
전체
부분
질료
미감
표상
기표
자리
심급
역할
난쟁이
자아
주체
긴장
쾌락
과잉
낙차
존재탐구
타자
소유
대화
```

Kotlin/JVM 자체의 기술적 타입은 그대로 사용한다.

예:

```kotlin
List<부분>
Set<심급>
Map<기표, 자리>
```

철학적 개념을 영어로 번역해서 임의의 기존 철학 개념과 동일시하지 않는다.

예를 들어 `되는것`을 자동으로 `Becoming`, `심급`을 `Agency` 등으로 번역하지 않는다.

번역은 별도의 철학적 결정이다.

---

# 7. 코드와 철학적 주석

모든 핵심 domain type에는 철학적 주석을 작성한다.

주석은 단순 API 설명이 아니다.

가능하면 다음을 기록한다.

1. 이 개념이 무엇인가
2. 무엇이 아닌가
3. 왜 필요한가
4. 무엇을 전제하는가
5. 어떤 개념이 이것을 필요로 하는가
6. 어떤 장과 관련되는가
7. 현재 확정된 내용과 미확정 내용을 구분한다

예:

```kotlin
/**
 * 전체
 *
 * 부분들의 단순 집합과 동일하지 않다.
 *
 * 전체성은 부분들을 하나의 전체로 지속시키는 형식이며,
 * 부분들은 전체가 형성된 뒤에도 각자의 운동 방향을 완전히 잃지 않는다.
 *
 * 따라서 전체의 지속과 부분의 독립적 운동 사이에는
 * 긴장이 발생할 수 있다.
 *
 * 관련:
 * - 1장: 되는 것의 내부 구조
 * - 6장: 전체와 부분
 *
 * 주의:
 * 생물학적 유기체에 대한 직접적인 경험과학 명제로
 * 자동 해석하지 않는다.
 */
```

---

# 8. 현재 철학의 최상위 형식

현재 프로젝트에서 가장 중요한 메타 형식은 다음과 같다.

> 어떤 것은 다른 것이 되려고 하지만 완전히 일치하지 못하고, 그 실패에서 새로운 차이와 운동이 발생한다.

간략한 형식:

```text
A
→ B가 되려고 함
→ B와의 완전한 일치 실패
→ C
→ 새로운 됨
```

중요:

C는 A가 처음부터 목표로 삼았던 결과가 아니다.

A는 C가 되려고 한 것이 아니라 B가 되려고 했다.

새로움은 목표한 새로움이 아니라 실패에서 발생한다.

완전한 일치가 성공한다면 운동이 종료될 수 있다.

따라서 실패는 단순한 결함이 아니라 다음 운동의 조건이 될 수 있다.

---

# 9. 규격과 방향성

됨은 완전한 무작위가 아니다.

그러나 규격이 최종 결과를 미리 결정하는 것도 아니다.

현재 핵심 명제:

> 규격은 무엇이 될지를 정하지 않고 무엇을 향해 실패할지를 정한다.

Agent는 이를 목적론적 최종 결과 결정과 혼동하지 않는다.

---

# 10. 전체와 부분

6장의 핵심 구조다.

기본 형식:

```text
부유하는 부분들
→ 우연한 전체성 형성
→ 각 부분의 독립적 운동
→ 전체의 지속을 위한 저항
→ 긴장
→ 보상
→ 안정화
```

전체는 부분의 단순한 합이 아니다.

부분은 전체에 포함된 이후에도 자신의 운동 방향을 가진다.

전체는 자신의 지속을 위해 부분의 운동에 반대 방향의 힘을 가할 수 있다.

이 관계에서 긴장이 발생한다.

현재 모델의 예:

```text
total {
    m1(=>)!<=,
    m2(<=)!=>,
    m3(=>)!<=,
    m4(<=)!=>
}
```

이것은 경험과학적 물리 모델로 자동 해석하지 않는다.

구성적 형식 모델이다.

---

# 11. 1장과 6장의 관계

6장의 전체성은 1장의 `되는 것`의 내부를 여는 구조로 이해한다.

1장에서는 A를 하나의 단위처럼 다룰 수 있다.

```text
A → B → C
```

그러나 6장에서는 A 자체가 이미 다음과 같은 전체일 수 있음이 드러난다.

```text
A = total {
    m1 →,
    m2 ←,
    m3 →,
    m4 ←
}
```

따라서 되는 주체 자체가 처음부터 완전히 단일하지 않을 수 있다.

6장은 1장을 폐기하지 않는다.

6장은 1장에서 black box로 사용했던 A의 내부 구조를 연다.

---

# 12. 물탱크

힘/욕망/운동은 여러 구조적 층을 지나면서 변형될 수 있다.

물탱크는 단순한 억압 장치가 아니다.

현재 핵심 명제:

> 물탱크는 물줄기를 없애지 않는다.
> 물줄기가 자신이 향하던 곳에 도달했다고 해석할 수 있는 경로를 제공한다.

따라서 규범과 언어는 욕망을 단순히 막는 것이 아니라 대체 경로와 해석 가능성을 제공할 수 있다.

원래의 최초 방향은 직접적으로 알 수 없다는 점을 유지한다.

---

# 13. 언어

## 13.1 단어의 도구목적성

단어의 경계는 사물의 자연적 경계를 그대로 복제한다고 가정하지 않는다.

현재 명제:

> 단어는 무엇이 본래 무엇인지를 나타내기보다,
> 그것을 어떻게 다룰 것인가에 따라 경계를 만든다.

---

## 13.2 단어의 비정의성

단어의 의미를 최종적인 정의로 완전히 고정할 수 있다고 가정하지 않는다.

정의는 다시 다른 단어를 호출한다.

이 구조는 됨의 실패 구조와 형식적 유사성을 가질 수 있다.

그러나 유사성이 곧 동일성이나 인과관계를 뜻하지 않는다.

---

## 13.3 단어와 차이

현재 명제:

> 외부에 차이가 있기 때문에 단어가 생기는 것이 아니라,
> 인간에게 유효한 차이가 발생할 때 단어의 자리가 분화한다.

---

## 13.4 자리와 기표

의미적 자리는 특정 기표와 동일하지 않다.

기표가 떠오르지 않아도 어떤 의미적 위치를 알고 있을 수 있다.

따라서 다음을 자동 동일시하지 않는다.

```text
자리 == 기표
```

---

# 14. 심급

현재 잠정 정의:

> 심급은 특정한 질료가 부재하거나 교체되어도
> 다른 것들과의 차이와 관계를 통해 유지되는 형식적 자리다.

심급은 물탱크와 동일하지 않다.

* 물탱크: 힘을 제한하거나 변형하는 층
* 심급: 관계 속에서 지속되는 형식적 자리

Agent는 둘을 편의를 위해 합치지 않는다.

---

# 15. 자아, 역할, 주체

현재 핵심 정의:

> 자아는 역할의 총합이다.

역할은 단순한 사회학적 직업 역할에 한정되지 않는다.

역할은 자기 자신을 다루기 위해 언어가 자신에게 적용한 도구목적적 구획이다.

현재 표현:

> 자아란 자신을 다루기 위해 언어가 자신에게 부여한
> 도구목적적 구획들의 총합이다.

주체는 긍정적 실체로 정의하지 않는다.

현재 핵심:

> 주체 = 저항감

더 정확히:

> 자아는 나를 사용할 수 있도록 구획한 역할들의 총합이고,
> 주체는 그 구획에 완전히 들어맞지 않는 데서 발생하는 저항감이다.

주체에게 고정된 긍정적 내용을 부여하면 그 내용 자체가 새로운 역할이 될 수 있다.

---

# 16. 난쟁이

난쟁이는 모든 부분 또는 모든 matter와 동일하지 않다.

난쟁이는 충분히 독립적인 목적, 판단, 감성, 발화 가능성을 획득한 심급을 의인화하여 다루는 모델이다.

현재 구별:

```text
심급 = 형식적/관계적 자리
난쟁이 = 독립적인 목소리와 방향성을 가진 심급의 작동 모델
```

모든 힘이 난쟁이가 되는 것은 아니다.

언어화되지 않거나 역할화되지 않은 힘이 존재할 수 있다.

---

# 17. 의식

의식을 반드시 하나의 중앙 통제자가 생산한다고 가정하지 않는다.

현재 모델에서는 여러 난쟁이의 상호작용 또는 대화 중 일부가 의식적으로 나타날 수 있다.

따라서 다음과 같은 모델을 탐구할 수 있다.

```text
난쟁이 A ─┐
난쟁이 B ─┼→ 대화 → 의식
난쟁이 C ─┘
```

그러나 이것을 실제 뇌의 물리적 구조라고 주장하지 않는다.

---

# 18. 자기타자화

전체가 자신의 부분을 관찰 대상으로 만들 수 있다.

예:

```text
observer -> total { m2 }
```

부분이 관찰 대상이 되는 순간, 관찰의 층위에서는 그 부분이 새로운 하나의 전체처럼 취급될 수 있다.

현재 중요한 가능성:

```text
부분
→ 관찰 대상
→ 표상
→ 차이의 포착
→ 언어적 자리
→ 역할
→ 난쟁이
```

단, 이 모든 화살표가 동일한 종류의 인과관계라고 가정하지 않는다.

각 변환의 종류는 별도로 정의되어야 한다.

---

# 19. 긴장, 스트레스, 쾌락

긴장은 독립적인 물질이나 실체로 취급하지 않는다.

현재 모델에서는 부분의 운동과 전체의 지속 방향 사이의 관계에서 발생하는 상태다.

쾌락 역시 독립적인 궁극적 목적이라고 자동 가정하지 않는다.

현재 모델에서는 전체 유지에 필요한 긴장을 완화하거나 특정 운동을 강화하는 보상 구조로 다룰 수 있다.

그러나 생물학적 사실과 철학적 구성 모델을 구분한다.

---

# 20. 낙차

낙차는 단순히 객관적 사건의 종료와 동일하지 않다.

관련 심급이 긴장의 종료를 인식하는 것이 중요하다.

대략적인 구조:

```text
기저 긴장
→ 역할에 의한 원인/규범/종료조건 형성
→ 수행
→ 높은 긴장
→ 종료 인식
→ 낙차
→ 쾌락
```

Agent는 낙차를 일반적인 reward와 자동 동일시하지 않는다.

---

# 21. 존재탐구

존재탐구는 자기계발 기법이 아니다.

핵심은 행동 전에 자신을 특정한 역할이나 설명으로 완전히 고정하지 않는 데 있다.

현재 중요한 구조:

```text
행동
→ 경험
→ 사후적 언어화
```

가 가능하다.

반드시

```text
정체성 정의
→ 계획
→ 행동
```

이어야 하는 것은 아니다.

존재탐구는 "나는 무엇인가?"를 최종 정의하는 것보다 "나는 무엇을 실제로 할 수 있는가?"의 범위를 탐색한다.

---

# 22. 과잉

하나의 감정, 욕망, 역할, 심급 등이 전체를 과도하게 점유하는 상태를 과잉으로 다룬다.

목표는 모든 긴장과 모순의 제거가 아니다.

긴장과 저항은 주체 발생의 조건이 될 수 있다.

따라서 건강한 상태를 "갈등 없음"으로 자동 모델링하지 않는다.

---

# 23. 타자와 소유

소유는 외부의 타자를 내부의 자기 일부처럼 재배치하는 구조로 이해할 수 있다.

중요한 구별:

```text
실제 타자 != 타자에 대한 나의 표상
```

이 둘은 타입 수준에서도 가능한 한 구별한다.

Agent는 편의를 위해 둘을 동일 객체로 취급하지 않는다.

---

# 24. 타자를 안다는 것

현재 핵심 명제:

> 타자를 알아간다는 것은 타자를 내 형식에 맞추는 것이 아니라,
> 타자 때문에 내 형식이 계속 실패하도록 허용하는 것이다.

앎을 최종적인 완전한 소유 상태로 모델링하지 않는다.

타자와의 관계에서는 실패가 지속될 가능성을 보존한다.

---

# 25. 대화

현재 핵심:

> 소유 대신 대화한다.

좋은 대화를 상대방을 정확히 정의하여 종료시키는 것으로만 이해하지 않는다.

현재 중요한 명제:

> 좋은 대화는 상대를 잘 정의하는 것이 아니라,
> 정의가 너무 빨리 끝나는 것을 막는 것이다.

대화는 다음과 같은 반복 구조를 가질 수 있다.

```text
발화
→ 응답
→ 기존 표상의 실패
→ 수정
→ 다음 발화
```

---

# 26. 뒷담화 등 윤리적으로 복합적인 행동

어떤 행동이 기능을 가진다는 사실은 그 행동을 윤리적으로 정당화하지 않는다.

반대로 어떤 행동이 윤리적으로 문제가 있다는 사실도 그 행동이 실제 심리적·관계적 기능을 갖는다는 사실을 자동으로 제거하지 않는다.

반드시 구별한다.

```text
기능 설명 != 윤리적 정당화
윤리적 문제성 != 기능 부재
```

---

# 27. 현실윤리

이 프로젝트의 목표는 항상 합리적이고 모순 없는 인간을 만드는 것이 아니다.

인간은 서로 충돌하는 감정, 욕망, 역할, 심급을 가질 수 있다.

중요한 것은 어느 하나가 전체를 과도하게 독점하여 다른 행동 가능성을 완전히 제거하지 않는 것이다.

또한 모든 행동과 욕망의 윤리적 무게가 동일하다고 가정하지 않는다.

사소하고 가역적인 행동과 타인, 장기적 관계, 조직 등에 큰 영향을 주는 중대한 결정은 구별되어야 한다.

중대하고 비가역적인 결정일수록 사전적 이성의 개입이 중요할 수 있다.

---

# 28. 자유

자유를 다음과 같이 정의하지 않는다.

```text
의지만 있으면 무엇이든 할 수 있음
```

현재 핵심 방향:

> 특정 행동을 반복시키는 낙차와 다른 행동을 봉쇄하는 과잉을 충분히 줄여,
> 이전에는 불가능했던 행동이 실제 수행 가능한 영역에 들어오는 것.

따라서 자유는 선택 결과만의 문제가 아니다.

핵심 질문:

> 다른 선택도 실제로 수행 가능했는가?

---

# 29. 윤리의 최종 방향

윤리의 목적은 단순히 강한 윤리 명령을 만드는 것이 아니다.

"좋은 사람"이라는 역할을 더욱 강하게 만드는 것 역시 과잉이 될 수 있다.

중요한 것은 윤리적 행동 자체가 실제 선택지로 남아 있을 수 있는 인간의 상태다.

최종적인 질문은 다음과 연결된다.

> 어떻게 살아야 하는가?

보다 먼저,

> 나는 그렇게 살 수 있는가?

를 묻는다.

---

# 30. 동기 구조

현재 14장에서 탐구 중인 구조다.

아리스토텔레스의 네 원인을 그대로 재현하는 것이 아니라, 이를 변형한 구성 모델이다.

기본 요소:

```text
질료
목적
형상
작용
```

현재 중요한 방향:

* 질료에는 목적 이전의 다양성이 있을 수 있다.
* 하나의 목적에는 여러 형상이 가능하다.
* 하나의 형상은 여러 용도로 사용될 수 있다.
* 작용은 외부와의 상호작용 속에서 현상으로 나타난다.

현재 동기 유형은 잠정적으로 다음과 같이 탐구한다.

```text
0동기 = 존재
1동기 = 욕망
2동기 = 당위
3동기 = 현실
```

잠정적인 단절 모델:

```text
존재:
아직 고정된 단절 이전

욕망:
질료 → 목적 → 형상 // 작용

당위:
질료 // 목적 → 형상 → 작용

현실:
질료 → 목적 // 형상 → 작용
```

이 분류는 아직 연구 중이다.

Agent는 이를 확정 이론처럼 경직시키지 않는다.

---

# 31. 관계 타입은 객체 타입보다 중요할 수 있다

이 프로젝트에서 가장 중요한 연구 대상 중 하나는 개념 자체뿐 아니라 개념 사이의 관계다.

단일 `→`로 모든 관계를 표현하지 않는다.

예상되는 관계의 예:

```kotlin
sealed interface 관계<A, B>

interface 발생시킨다<A, B> : 관계<A, B>
interface 구성한다<A, B> : 관계<A, B>
interface 표상한다<A, B> : 관계<A, B>
interface 기표한다<A, B> : 관계<A, B>
interface 관찰한다<A, B> : 관계<A, B>
interface 저항한다<A, B> : 관계<A, B>
interface 형식적으로닮는다<A, B> : 관계<A, B>
```

실제 이름과 구조는 철학적 검토 후 결정한다.

Agent가 새로운 관계 타입이 필요하다고 판단하면 임의로 추가하지 말고 필요성을 설명한다.

---

# 32. 관계 합성

이 프로젝트의 핵심 연구 문제 중 하나다.

모든 관계가 자동으로 합성될 수 있다고 가정하지 않는다.

예:

```text
A --표상--> B
B --기표--> C
```

라고 해서 자동으로

```text
A --기표--> C
```

가 성립한다고 가정하지 않는다.

관계 합성에는 별도의 규칙이 필요하다.

가능한 형식:

```kotlin
fun <A, B, C> 합성(
    첫째: 관계<A, B>,
    둘째: 관계<B, C>
): 관계<A, C>?
```

그러나 nullable로 철학적 문제를 숨기지 않는다.

가능하면 합성 가능/불가능/미결정을 타입 수준에서 구별한다.

---

# 33. 인과와 층위변환을 구별한다

다음과 같은 연쇄를 모두 인과관계로 해석하지 않는다.

```text
matter
→ 감성
→ 표상
→ 기표
→ 역할
→ 난쟁이
→ 의식
```

각 화살표는 서로 다른 종류일 수 있다.

예:

* 발생
* 관찰
* 경험
* 표상
* 기표화
* 역할화
* 구성
* 형식적 동형
* 모델링
* 의인화

Agent는 `A -> B`를 발견하면 먼저 다음을 묻는다.

> 이 화살표는 정확히 어떤 관계인가?

---

# 34. 발생적 의존과 구성적 상호의존

모든 dependency가 일방향 DAG일 필요는 없다.

예:

```text
전체 ↔ 부분
기표 ↔ 다른 기표
역할 ↔ 다른 역할
난쟁이 ↔ 다른 난쟁이
나 ↔ 타자
발화 ↔ 응답
```

이들은 상호구성적일 수 있다.

따라서 순환참조 자체를 오류로 판단하지 않는다.

대신 다음을 검사한다.

> 이 순환은 서로를 구성하는 관계인가?

또는

> 설명되지 않은 것을 서로에게 떠넘기는 순환논증인가?

후자의 경우 철학적 컴파일 오류로 보고한다.

---

# 35. 동일성과 형식적 동형성을 구별한다

이 프로젝트에서는 서로 다른 질료에서 동일한 형식이 반복되는 경우가 중요하다.

예:

```text
matter들의 전체 ↔ 신체
난쟁이들의 전체 ↔ 자아/의식
```

이 둘이 동일한 실체라고 주장하지 않는다.

다만 다음 형식이 반복될 수 있다.

```text
부분의 독립적 운동
↔
전체의 지속
```

따라서 다음을 구별한다.

```text
동일하다
형식적으로 닮았다
구성적으로 대응한다
원인이다
표상한다
```

Agent는 형식적 유사성을 존재론적 동일성으로 승격시키지 않는다.

---

# 36. 질료와 형식

프로젝트 전체에서 중요한 방법론이다.

특정 질료를 제거했을 때에도 관계적 구조가 유지되는지 탐구한다.

예:

```text
Whole<Cell>
Whole<Dwarf>
```

두 구조가 같은 연산을 공유한다고 해서 Cell과 Dwarf가 동일한 것은 아니다.

중요한 질문은:

> 질료를 교체해도 어떤 관계 형식이 보존되는가?

이다.

---

# 37. 개념 삭제 테스트

핵심 개념의 필요성을 검사하기 위해 개념 삭제 사고실험 또는 테스트를 수행할 수 있다.

예:

```text
Role 제거
↓
Dwarf 구성 가능?
↓
Consciousness 구성 가능?
↓
Ought 동기 설명 가능?
```

개념 하나를 제거해도 체계가 아무 변화 없이 유지된다면 다음을 검토한다.

1. 이 개념이 중복인가?
2. 다른 개념의 별칭인가?
3. 아직 의존성이 코드에 충분히 표현되지 않은 것인가?

Agent는 자동으로 삭제하지 않는다.

결과를 사용자에게 보고한다.

---

# 38. 철학적 invariant

테스트는 철학적 invariant를 표현할 수 있다.

예:

```text
실제 타자 != 타자에 대한 표상
```

```text
부분의 단순 집합 != 전체성
```

```text
주체 != 고정된 긍정적 역할
```

```text
기능 설명 != 윤리적 정당화
```

```text
형식적 동형성 != 존재론적 동일성
```

```text
컴파일 가능성 != 경험적 진리
```

이러한 invariant는 가능한 경우 테스트 또는 타입 구조로 표현한다.

---

# 39. 테스트의 의미

테스트 성공은 철학적 진리의 증명이 아니다.

테스트는 다음만 의미한다.

> 현재 선언된 규칙 아래에서 이 invariant가 위반되지 않았다.

테스트 실패는 두 종류로 구분한다.

### 구현 실패

철학은 명확하지만 코드가 잘못됨.

→ Agent가 수정 가능.

### 철학적 실패

두 철학적 정의가 충돌하거나 필요한 관계가 정의되지 않음.

→ Agent가 임의 수정 금지.

사용자에게 보고.

---

# 40. 미결정 상태를 일급 객체로 취급한다

철학적으로 결정되지 않은 것을 억지로 `null`로 처리하지 않는다.

필요하다면 다음과 같은 명시적 상태를 고려한다.

```kotlin
sealed interface 철학적결정<out T> {
    data class 확정<T>(val 값: T) : 철학적결정<T>
    data class 잠정<T>(val 값: T) : 철학적결정<T>
    data class 미결정(val 이유: String) : 철학적결정<Nothing>
}
```

단, 이 구조 역시 필요성이 확인된 뒤 도입한다.

---

# 41. 확정 / 잠정 / 탐색을 구별한다

철학적 개념에는 성숙도가 다르다.

가능하면 문서 또는 코드 주석에서 다음을 구별한다.

```text
[확정]
현재 체계에서 강하게 유지해야 하는 정의

[잠정]
현재 가장 적절하지만 변경 가능성이 높은 정의

[탐색]
아이디어 수준이며 구조가 아직 결정되지 않음
```

Agent는 `[탐색]` 상태의 아이디어를 `[확정]` 타입으로 굳히지 않는다.

---

# 42. 장 구조와 코드 구조를 동일시하지 않는다

책의 장과 Kotlin package/class 구조는 서로 다른 목적을 가진다.

예를 들어 `언어적 자리`가 2장에서 처음 등장한다고 해서 반드시 `chapter02` package에 들어갈 필요는 없다.

책의 장은 독자의 이해 순서를 위한 구조다.

코드의 package는 철학적 의존성과 범주를 표현하기 위한 구조다.

따라서 다음과 같은 package를 우선 고려할 수 있다.

```text
되는것
├─ 존재
├─ 전체부분
├─ 감성
├─ 언어
├─ 자아
├─ 행동
├─ 타자
├─ 윤리
└─ 논리
```

실제 구조는 프로젝트 진행 중 변경 가능하다.

---

# 43. 책의 서술 순서와 논리적 의존 순서를 구별한다

책에서 먼저 등장한다고 논리적으로 더 근본적인 것은 아니다.

예를 들어:

* 1장은 독자에게 미래지향적인 "무엇이 되는가"를 먼저 보여준다.
* 6장은 1장에서 하나처럼 다뤘던 A의 내부를 연다.
* 2~5장은 6장의 인간적 분화를 설명하기 위한 언어, 역할, 심급 등의 도구를 제공한다.

따라서 독서 순서와 dependency graph를 동일시하지 않는다.

---

# 44. 선사용 / 후정당화를 허용한다

책에서는 어떤 개념을 먼저 경험적으로 보여주고 나중에 발생 구조를 설명할 수 있다.

예:

```text
2장: 언어적 자리의 현상을 먼저 보여줌
5장: 난쟁이 모델을 먼저 사용
6장: 왜 복수의 자리가 발생할 수 있는지 더 깊이 설명
```

이것은 자동으로 논리 오류가 아니다.

Agent는 다음을 구별한다.

```text
서술적 선행
논리적 선행
발생적 선행
구성적 상호의존
```

---

# 45. 나선형 구조

《되는 것》은 반드시 직선적인 증명 체계일 필요는 없다.

한 개념이 후반부에서 다시 나타나며 이전 의미가 확장될 수 있다.

예:

```text
1장: 되는 것
↓
6장: 되는 것의 내부 전체/부분
↓
후반: 타자와 윤리
↓
자유
↓
처음의 "된다"가 새로운 의미를 획득
```

따라서 재귀적·나선적 재해석을 오류로 취급하지 않는다.

단, 같은 용어의 의미가 변경되었다면 변경이 명시되어야 한다.

---

# 46. 철학적 버전 관리

철학적 정의가 변경될 경우 단순 리팩터링으로 취급하지 않는다.

가능하면 commit 또는 변경 기록에서 다음을 명시한다.

```text
[철학적 변경]
심급의 정의 변경

이전:
...

이후:
...

변경 이유:
...

영향:
- 역할
- 난쟁이
- 의식
- 동기론
```

Agent가 git commit을 작성할 권한이 있는 환경이라면 철학적 변경과 단순 구현 변경을 가능한 한 구분한다.

---

# 47. 리팩터링 원칙

Agent는 일반적인 "clean code"를 이유로 철학적 구별을 제거하지 않는다.

두 타입이 구현상 동일하더라도 철학적으로 다른 개념이라면 분리 상태를 유지할 수 있다.

예:

```text
표상
기표
자리
```

가 현재 구현상 모두 문자열 하나를 감싸더라도 하나의 `String`으로 합치지 않는다.

철학적 차이가 타입 차이보다 우선한다.

---

# 48. 추상화 원칙

중복 코드를 발견했다고 즉시 추상화하지 않는다.

이 프로젝트에서는 반복이 아직 발견되지 않은 차이를 보존하고 있을 수 있다.

추상화하기 전에 다음을 확인한다.

> 두 구조는 정말 같은 형식인가?

> 아니면 현재 코드만 우연히 비슷한가?

철학적으로 확인되지 않은 추상화는 금지한다.

---

# 49. DRY 원칙의 제한

일반 소프트웨어의 DRY 원칙을 절대적 규칙으로 적용하지 않는다.

철학적 차이를 드러내기 위해 의도적인 중복을 허용한다.

중복 제거는 의미 동일성이 확인된 뒤 수행한다.

---

# 50. 이름 변경

철학적 domain type의 이름 변경은 의미 변경일 수 있다.

Agent는 단순 코드 스타일을 이유로 핵심 개념명을 임의 변경하지 않는다.

예:

```text
심급 → Agency
되는것 → Becoming
미감 → AestheticSense
```

등을 자동 수행하지 않는다.

---

# 51. 외부 철학자의 개념을 자동 도입하지 않는다

프로젝트는 여러 철학적 전통과 유사성을 가질 수 있다.

그러나 Agent는 다음과 같이 자동 치환하지 않는다.

```text
사용자의 "심급" = 라캉의 X
사용자의 "주체" = 사르트르의 Y
사용자의 "전체" = 헤겔의 Z
```

유사성은 참고사항일 뿐 동일성이 아니다.

외부 철학자의 개념을 코드의 기반 타입으로 도입하려면 사용자의 명시적 결정이 필요하다.

---

# 52. 경험과학과 구성 모델을 구별한다

유전자, 신체, 긴장, 쾌락, 충동 등의 용어가 등장하더라도 이를 자동으로 생물학·신경과학의 직접적 사실 주장으로 변환하지 않는다.

이 프로젝트의 많은 구조는 구성적 모델이다.

Agent가 경험과학적 주장을 추가하려면 별도의 근거와 사용자 승인이 필요하다.

---

# 53. AI/Agent가 해서는 안 되는 철학적 최적화

Agent는 체계를 더 단순하게 만들기 위해 다음을 하지 않는다.

* 모순되는 욕망 제거
* 하나의 최고심급으로 통합
* 하나의 중앙 자아 도입
* 모든 행동을 효용 극대화로 환원
* 모든 동기를 보상으로 환원
* 모든 언어를 정보 전달로 환원
* 모든 윤리를 규칙 준수로 환원
* 모든 자유를 선택 횟수로 환원

이러한 환원은 현재 프로젝트의 핵심 방향과 충돌할 수 있다.

---

# 54. 현실적 인간의 모순을 보존한다

이 프로젝트에서 모순되는 감정이나 행동은 반드시 오류가 아니다.

한 사람이:

* 누군가를 싫어하면서도
* 그 사람의 능력을 인정하고
* 그 사람에 대해 험담하면서도
* 중요한 상황에서는 그 사람이 적합하다고 판단할 수 있다.

이러한 상태를 하나의 일관된 감정으로 강제 정규화하지 않는다.

중요한 것은 어느 한 심급이 전체를 과잉 점유하는가이다.

---

# 55. 사후적 이성

이성은 모든 행동을 사전에 검열해야 하는 중앙 관리자로 모델링하지 않는다.

다음 순환을 허용한다.

```text
행동
→ 사후적 성찰
→ 형식 발견
→ 다음 상황에서의 사전 판단 가능성 증가
```

단, 중대하고 비가역적이며 타자에게 큰 영향을 주는 행동에서는 사전적 이성의 중요성이 커질 수 있다.

---

# 56. 프로젝트의 최종 철학적 방향

현재 《되는 것》의 시작과 끝을 잇는 문장:

> 우리는 원하는 것이 되지 못한다.
> 그러므로 아직 다른 것이 될 수 있다.

Agent는 이 문장을 단순한 낙관주의 문구로 해석하지 않는다.

첫 문장은 됨의 실패 구조를 가리킨다.

두 번째 문장은 바로 그 실패 때문에 완전한 고정이 이루어지지 않았고 다른 운동 가능성이 남아 있음을 가리킨다.

---

# 57. Agent의 질문 기준

Agent가 철학적 공백을 발견했을 때 무조건 사소한 질문을 남발하지 않는다.

다음 경우에는 스스로 구현할 수 있다.

* 이미 확정된 정의의 직접적 구현
* 명백한 코드 오류
* 이름 충돌
* import
* 테스트 프레임워크 설정
* 철학적 의미를 바꾸지 않는 기계적 리팩터링

다음 경우에는 사용자 판단이 필요하다.

* 새로운 철학적 관계가 필요함
* 기존 두 개념을 합쳐야 컴파일됨
* 기존 개념을 분리해야 함
* 인과인지 동형성인지 불명확함
* 발생 관계인지 관찰 층위 변환인지 불명확함
* 순환 의존이 상호구성인지 순환논증인지 불명확함
* 새로운 전제가 없으면 체계가 연결되지 않음
* 기존 확정 정의와 새로운 구조가 충돌함
* 윤리적 의미가 변함

---

# 58. Agent의 보고 형식

철학적 문제를 발견하면 가능하면 다음 형식을 사용한다.

```text
[철학적 컴파일 문제]

위치:
전체 -> 자기타자화 -> 표상

현재 정의:
- 전체는 부분들을 포함한다.
- 관찰자는 전체의 부분을 관찰할 수 있다.
- 표상은 관찰된 대상을 내부적으로 나타낸다.

필요한 연결:
관찰된 부분 -> 표상

문제:
현재 정의만으로는 관찰이 반드시 표상을 발생시키는지 알 수 없다.

가능한 선택:
A. 모든 관찰은 표상을 생성한다.
B. 특정 조건을 만족하는 관찰만 표상을 생성한다.
C. 관찰과 표상을 별개의 층위로 유지한다.

Agent 판단:
결정하지 않음.

영향 범위:
- 기표화
- 언어적 자리
- 역할
- 난쟁이
```

가능한 선택지를 제시할 수 있지만 임의로 선택하지 않는다.

---

# 59. 구현 제안과 철학적 결정을 분리한다

Agent가 제안할 때 다음을 구분한다.

```text
[구현 제안]
sealed interface가 현재 구조를 표현하기 적절해 보임.

[철학적 결정 필요]
표상과 기표 사이의 변환이 단방향인지 여부.
```

기술적 권고를 철학적 사실처럼 표현하지 않는다.

---

# 60. 최초 개발 우선순위

전체 14장을 한 번에 구현하지 않는다.

우선 6장의 최소 구조를 테스트베드로 삼는다.

권장 초기 순서:

```text
부분
↓
전체
↓
부분의 운동
↓
전체의 지속
↓
저항
↓
긴장
↓
보상
↓
안정
↓
자기관찰
↓
자기타자화
↓
표상
```

그 이후 철학적으로 필요한 경우:

```text
차이
↓
자리
↓
기표
↓
역할
↓
심급
↓
난쟁이
↓
대화
↓
의식
```

를 연결한다.

단, 이 순서는 구현 편의를 위한 잠정 순서이며 철학적 발생 순서로 자동 확정하지 않는다.

---

# 61. 초기 성공 기준

프로젝트 초기 성공은 "AI가 사람을 분석할 수 있음"이 아니다.

다음 정도면 성공이다.

1. 핵심 개념이 타입으로 구분된다.
2. 관계 종류가 명시된다.
3. 임의의 `→`가 감소한다.
4. 미결정 연결이 눈에 보인다.
5. 한 개념의 변경이 어디까지 영향을 주는지 추적할 수 있다.
6. 철학적 invariant가 테스트로 표현된다.
7. 컴파일 실패가 철학적 질문으로 번역된다.
8. 코드베이스가 현재 《되는 것》의 dependency graph 역할을 한다.

---

# 62. AI 모델화는 후순위다

향후 이 프로젝트가 다음과 같은 구조로 확장될 가능성은 있다.

```text
자연어
↓
LLM
↓
《되는 것》 내부 표현
↓
Kotlin 추론 엔진
↓
구조적 결과
↓
LLM
↓
자연어
```

또는 난쟁이들을 독립 agent로 모델링할 가능성도 있다.

그러나 현재 단계에서 AI를 만드는 것이 프로젝트의 목적은 아니다.

AI 구현 요구가 철학을 역으로 변형시키지 않도록 한다.

순서:

```text
철학
→ 형식화
→ 컴파일
→ 테스트
→ 추론
→ 필요할 경우 AI
```

이지,

```text
AI를 만들기 위해 철학을 변경
```

이 아니다.

---

# 63. 보안 및 프로젝트 경계

이 프로젝트는 사용자의 개인 철학 및 저작 프로젝트다.

회사 업무용 코드나 회사 소유 자료와 혼합하지 않는다.

Agent는 사용자의 명시적 요청 없이 외부 저장소, 회사 시스템, 제3자 서비스 등에 프로젝트 내용을 복사하거나 게시하지 않는다.

개인 저작물이라는 경계를 유지한다.

---

# 64. Agent가 항상 기억해야 할 질문

코드를 추가하거나 변경하기 전에 가능한 경우 다음을 확인한다.

### 개념

* 이것은 새로운 개념인가?
* 기존 개념과 정말 다른가?
* 무엇이 이것을 필요로 하는가?

### 관계

* A에서 B로 가는 화살표는 무엇인가?
* 인과인가?
* 구성인가?
* 표상인가?
* 기표화인가?
* 관찰인가?
* 형식적 동형인가?
* 단순한 서술 순서인가?

### 층위

* 존재론인가?
* 감성의 층위인가?
* 언어의 층위인가?
* 역할의 층위인가?
* 의식의 층위인가?
* 윤리의 층위인가?

### 의존성

* 이 개념은 무엇을 전제하는가?
* 이것을 제거하면 무엇이 무너지는가?
* 순환한다면 정당한 상호구성인가?

### 철학적 안전성

* 컴파일시키기 위해 새로운 철학을 몰래 추가하고 있지 않은가?
* 두 개념을 편의상 동일시하고 있지 않은가?
* 형식적 유사성을 존재론적 동일성으로 바꾸고 있지 않은가?
* 구성 모델을 경험적 사실처럼 만들고 있지 않은가?

---

# 65. 최상위 Agent 원칙

이 프로젝트에서 Agent의 가장 중요한 행동 원칙은 다음 한 문장으로 요약한다.

> 구현할 수 있는 것은 구현하고,
> 형식화할 수 있는 것은 형식화하고,
> 검사할 수 있는 것은 검사하되,
> 철학적으로 결정되지 않은 것은 결정된 것처럼 만들지 않는다.

그리고 가장 중요한 실패 처리 원칙:

> 코드가 컴파일되지 않을 때 먼저 코드를 의심하라.
> 코드가 맞다면 철학의 연결을 조사하라.
> 철학적 연결이 없다면 그것을 임의로 만들지 말고 드러내라.

---

# 66. 프로젝트의 궁극적 컴파일 조건

《되는 것》이 이 프로젝트 안에서 "컴파일된다"는 것은 현실에 대한 진리가 증명되었다는 뜻이 아니다.

다음을 의미한다.

> 각 개념이 자신의 범주를 유지하고,
> 필요한 다른 개념과 명시적으로 연결되며,
> 허용된 관계를 통해 서로를 요구하고,
> 순환하는 경우 그 순환의 성격이 설명되며,
> 정의되지 않은 도약이 숨겨져 있지 않고,
> 핵심 개념을 임의로 삭제하거나 합치지 않은 상태에서,
> 전체 체계가 하나의 구성으로 성립한다.

이것이 이 프로젝트에서 말하는 **철학적 컴파일**이다.
